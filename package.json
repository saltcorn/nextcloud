{
  "name": "@saltcorn/nextcloud",
  "version": "0.3.0",
  "description": "Nextcloud Talk integration: reliable sending with fallback, direct messages, threads, directory search, chat monitoring, agent skill",
  "main": "index.js",
  "scripts": {
    "test": "jest tests --runInBand"
  },
  "dependencies": {},
  "author": "Tom Nielsen",
  "license": "MIT",
  "repository": "github:saltcorn/nextcloud",
  "publishConfig": {
    "access": "public"
  },
  "eslintConfig": {
    "extends": "eslint:recommended",
    "parserOptions": {
      "ecmaVersion": 2020
    },
    "env": {
      "node": true,
      "es6": true
    },
    "rules": {
      "no-unused-vars": "off",
      "no-case-declarations": "off",
      "no-empty": "warn",
      "no-fallthrough": "warn"
    }
  }
}
